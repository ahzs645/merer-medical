# Lab Plausibility Imports

These JSON files are review outputs for biologic plausibility and administrative range checks.
They are intentionally separate from `referenceStandards` because they are not clinical normal
reference intervals and should not drive high/low interpretation in the labs UI.

Regenerate with:

```sh
npm run labs:nonclinical
```

