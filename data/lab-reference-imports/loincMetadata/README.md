# Lab LOINC Metadata Imports

These JSON files are review outputs for LOINC aliases, units, mappings, and CodeSystem metadata.
They are intentionally separate from `referenceStandards` because they do not define clinical
normal ranges.

Regenerate with:

```sh
npm run labs:nonclinical
```

